node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build
/dist

# OS / editor cruft
.DS_Store
Thumbs.db
*~

# Local Claude Code settings (user/machine-specific)
.claude/settings.local.json

# Secrets & environment — NEVER commit
.env
.env.*
!.env.example
!.env.test
/secrets
/config/*.local.json
# Debug SSH keypair (temporary remote-debug access to a dev's own Pi)
/.debug-keys

# Local data & databases (stay on-device, encrypted at rest)
/data
# Storage-location pointer (local vs NAS)
.data-location
*.db
*.db-journal
*.db-wal
*.db-shm

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
